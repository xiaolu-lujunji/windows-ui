import IconBase from './icon-base';
import styled from '../styles/styled';

const ChevronRightMed = styled(IconBase)({
  '&::before': {
    content: '"\\e974"',
  },
});

export default ChevronRightMed;

{
  "name": "@wui/monorepo",
  "description": "Implements Microsoft's Windows UI Library in React.",
  "license": "MIT",
  "scripts": {
    "test": "cd apps/test && npm run cypress:open",
    "docs": "cd apps/docs && npm run dev",
    "build:docs": "cd apps/docs && npm run build"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.2.0",
    "@commitlint/config-conventional": "^17.2.0",
    "husky": "^8.0.1"
  }
}

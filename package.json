{
  "name": "win-ui-monorepo",
  "description": "Implements Microsoft's Windows UI Library in React.",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^1.4.3"
  }
}
